"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const transaction_1 = require("./transaction");
function supportTransaction(Assertion) {
    Assertion.addMethod('transaction', function (cmp) {
        const result = (0, transaction_1.compareTransactionForTest)(this._obj, cmp);
        this.assert(result.pass, result.posMessage, result.negMessage);
    });
}
try {
    const chai = require("chai");
    if (chai)
        chai.use((chai) => {
            supportTransaction(chai.Assertion);
        });
}
catch (e) { }
